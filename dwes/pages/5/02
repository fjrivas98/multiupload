<div align="left">
   &lt;button&gt; is a clickable button, which can be used in forms, or anywhere in a document that needs simple, standard button functionality.
   <hr>
   &lt;datalist&gt; contains a set of &lt;option&gt; elements that represent the values available for other controls.
   <hr>
   &lt;fieldset&gt; is used to group several controls as well as labels within a web form.
   <hr>
   &lt;form&gt; a document section that contains interactive controls for submitting information to a web server.
</div>